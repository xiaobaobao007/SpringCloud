spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaobaobao007/SpringCloud/
          search-paths: resources
          username:
          password:
server:
  port: 8086