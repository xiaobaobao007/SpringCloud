server:
  port: 8086
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaobaobao007/SpringCloud
          search-paths: blob/master/Config/src/main/resources/
          username:
          password: