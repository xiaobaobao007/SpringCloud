spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fymod/easy-spring-cloud/
          search-paths: springcloud-config
          username:
          password:
server:
  port: 8086